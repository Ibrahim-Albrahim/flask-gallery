import ReactLoading from 'react-loading';

const Loading = () => {
    return  <ReactLoading type="spinningBubbles" color="#ffffff" height={300} width={300} className="spinning-bubbles"/>
};
export default Loading;